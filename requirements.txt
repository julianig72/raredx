requests>=2.28
github-copilot-sdk>=1.0,<2

# Optional AI modules:
# torch>=2.0        # for --esm (ESM-2 missense scoring)
# fair-esm>=2.0     # for --esm
# anthropic>=0.40   # for --clinical-note (LLM HPO extraction)
