# raredx web backend
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
requests>=2.31
github-copilot-sdk>=1.0,<2

# Optional AI layers (enable with --esm / the ESM toggle):
#   torch>=2.0
#   fair-esm>=2.0
# Optional Anthropic fallback:
#   anthropic>=0.25   (+ ANTHROPIC_API_KEY)
# AlphaMissense needs NO extra deps (precomputed scores via Ensembl VEP).

# Test-only (for web/test_server.py; not needed to run the server):
#   pytest>=8.0
#   httpx>=0.27        # required by fastapi.testclient.TestClient
